# Data Quality Report

**Project:** Enterprise Supply Chain Control Tower  
**Phase:** 2 — Data Audit  
**Generated by:** `Python/01_data_audit.py`  
**Source:** `Data/Raw/` (10 files)

> **Phase 3 follow-up.** Every finding below was carried into the SQL
> layer and re-tested there. `SQL/08_validation.sql` runs 66 checks
> against the loaded database and confirms each one independently —
> including the calendar overflow (now impossible: 13 date foreign keys
> enforce it), the open-order date ambiguity (now split into mutually
> exclusive columns), the derivability of `InventoryValue` (re-proved
> against the raw staged value), and the baseline forecast benchmark
> (re-derived in T-SQL at WAPE 13.04%, matching this report exactly).
> Result: 66 passed, 0 failed. See `SQL/README.md`.

## Severity definitions

| Severity | Meaning |
|---|---|
| **ERROR** | A genuine data fault requiring correction in the SQL layer. |
| **ANOMALY** | Suspicious; requires a documented decision before it is trusted. |
| **EXCEPTION** | Valid business behaviour that merely appears unusual. |
| **INFO** | Context that shapes a downstream modelling decision. |

## Summary

| Severity | Count |
|---|---|
| ERROR | 6 |
| ANOMALY | 0 |
| EXCEPTION | 7 |
| INFO | 52 |

## Table inventory

| Table | Rows | Columns |
|---|---|---|
| `dim_date` | 1,704 | 9 |
| `dim_product` | 1,000 | 8 |
| `dim_supplier` | 120 | 4 |
| `dim_warehouse` | 6 | 3 |
| `fact_inventory_snapshot` | 315,000 | 6 |
| `fact_purchase_orders` | 25,000 | 11 |
| `fact_sales_orders` | 80,000 | 10 |
| `fact_shipments` | 80,000 | 8 |
| `fact_weekly_demand` | 315,000 | 5 |
| `security_user_access` | 7 | 3 |
| **Total** | **817,837** | |

## ERROR findings

| Table | Check | Detail | Action |
|---|---|---|---|
| `fact_purchase_orders` | Calendar coverage | ExpectedReceiptDate: 318 rows fall outside dim_date (max 2026-09-29 vs calendar max 2026-08-31) | Extend the calendar. The supplied calendar was built to the as-of date and ignores the forward horizon of in-flight orders. |
| `fact_purchase_orders` | Calendar coverage | ActualReceiptDate: 368 rows fall outside dim_date (max 2026-10-07 vs calendar max 2026-08-31) | Extend the calendar. The supplied calendar was built to the as-of date and ignores the forward horizon of in-flight orders. |
| `fact_sales_orders` | Calendar coverage | PromisedDeliveryDate: 327 rows fall outside dim_date (max 2026-09-09 vs calendar max 2026-08-31) | Extend the calendar. The supplied calendar was built to the as-of date and ignores the forward horizon of in-flight orders. |
| `fact_sales_orders` | Calendar coverage | ActualDeliveryDate: 435 rows fall outside dim_date (max 2026-09-16 vs calendar max 2026-08-31) | Extend the calendar. The supplied calendar was built to the as-of date and ignores the forward horizon of in-flight orders. |
| `fact_shipments` | Calendar coverage | ShipDate: 55 rows fall outside dim_date (max 2026-09-01 vs calendar max 2026-08-31) | Extend the calendar. The supplied calendar was built to the as-of date and ignores the forward horizon of in-flight orders. |
| `fact_shipments` | Calendar coverage | DeliveryDate: 435 rows fall outside dim_date (max 2026-09-16 vs calendar max 2026-08-31) | Extend the calendar. The supplied calendar was built to the as-of date and ignores the forward horizon of in-flight orders. |

## EXCEPTION findings

| Table | Check | Detail | Action |
|---|---|---|---|
| `cross-table` | Product coverage | dim_product holds 1,000 products. Sales reference 1,000, purchase orders 1,000, weekly demand 500, inventory 500 | Half the catalogue has no inventory or demand signal. Add a HasDemandSignal flag to DimProduct and disclose the gap in the report rather than filtering silently. |
| `cross-table` | Demand vs sales reconciliation | Over the shared window, weekly demand totals 6,219,403 units against 152,681 sales-order units (ratio 40.7x) | The two series are independent in this synthetic dataset. Never present demand as derived from sales; document them as separate measures. |
| `fact_purchase_orders` | Status vs dates | 368 records are flagged Open and every one of them - and only them - has a receipt date after the as-of date 2026-08-31. Status is a deterministic restatement of 'scheduled in the future', so these are in-flight orders, not defects. Their ActualReceiptDate is an EXPECTED date. | Split into ActualDate (NULL when open) and ScheduledDate in the SQL view. Exclude open records from OTIF: they are not yet due, so scoring them as on-time or late would be wrong. |
| `fact_purchase_orders` | Derivable column | POValue differs from QuantityOrdered x UnitCost on 24,998 of 25,000 rows (99.99%); median deviation 268.97 | Purchase price variance is a genuine procurement metric; retain and expose as Purchase Price Performance (Requirement 10). |
| `fact_sales_orders` | Status vs dates | 435 records are flagged Open and every one of them - and only them - has a delivery date after the as-of date 2026-08-31. Status is a deterministic restatement of 'scheduled in the future', so these are in-flight orders, not defects. Their ActualDeliveryDate is an EXPECTED date. | Split into ActualDate (NULL when open) and ScheduledDate in the SQL view. Exclude open records from OTIF: they are not yet due, so scoring them as on-time or late would be wrong. |
| `fact_sales_orders` | Derivable column | Revenue differs from Quantity x UnitPrice on 79,958 of 80,000 rows (99.95%); median deviation 13.94 | Discounting or price variation is normal; retain Revenue as stored. |
| `security_user_access` | Sentinel value | WarehouseID uses the literal 'ALL' for the Supply Director role | RLS predicate must special-case 'ALL'; do not treat as an orphan key. |

## INFO findings

| Table | Check | Detail | Action |
|---|---|---|---|
| `cross-table` | As-of date | Latest transaction date across all facts is 2026-08-31; this is the extract's as-of date and the anchor for all 'current' calculations | Expose as an explicit model anchor rather than hard-coding TODAY() or a literal date in individual measures. |
| `cross-table` | Product coverage overlap | Demand and inventory cover the same product sets (500 in common) |  |
| `cross-table` | Week grid alignment | Demand and inventory week grids are identical | Safe to join demand to inventory on week. |
| `dim_date` | Primary key uniqueness | Date is unique across 1,704 rows | Safe to enforce as PRIMARY KEY. |
| `dim_date` | Calendar span | 2022-01-01 to 2026-08-31 (1,704 days) |  |
| `dim_date` | Required calendar horizon | The furthest forward date in any fact table is 2026-10-07, 37 days beyond the supplied calendar | Rebuild the calendar to cover this horizon plus the forecast window. |
| `dim_product` | Primary key uniqueness | ProductID is unique across 1,000 rows | Safe to enforce as PRIMARY KEY. |
| `dim_product` | Referential integrity | PrimarySupplierID fully resolves to dim_supplier.SupplierID | Safe to enforce as FOREIGN KEY. |
| `dim_supplier` | Primary key uniqueness | SupplierID is unique across 120 rows | Safe to enforce as PRIMARY KEY. |
| `dim_warehouse` | Primary key uniqueness | WarehouseID is unique across 6 rows | Safe to enforce as PRIMARY KEY. |
| `fact_inventory_snapshot` | Primary key uniqueness | SnapshotDate+ProductID+WarehouseID is unique across 315,000 rows | Safe to enforce as PRIMARY KEY. |
| `fact_inventory_snapshot` | Referential integrity | ProductID fully resolves to dim_product.ProductID | Safe to enforce as FOREIGN KEY. |
| `fact_inventory_snapshot` | Referential integrity | WarehouseID fully resolves to dim_warehouse.WarehouseID | Safe to enforce as FOREIGN KEY. |
| `fact_inventory_snapshot` | Derivable column | InventoryValue differs from OnHandUnits x UnitCost on 0 of 315,000 rows (max deviation 0.00) | Column is fully derivable and high-cardinality; candidate for removal in Phase 24 optimisation. |
| `fact_inventory_snapshot` | Grain completeness | 105 weeks x 500 products x 6 warehouses = 315,000 expected; 315,000 actual (complete dense grid) | A dense grid means no gap-filling is required before forecasting. |
| `fact_inventory_snapshot` | Week alignment | SnapshotDate falls on: ['Monday'] |  |
| `fact_inventory_snapshot` | History span | SnapshotDate spans 2024-09-02 to 2026-08-31 (2.0 years) |  |
| `fact_purchase_orders` | Primary key uniqueness | PurchaseOrderID is unique across 25,000 rows | Safe to enforce as PRIMARY KEY. |
| `fact_purchase_orders` | Referential integrity | ProductID fully resolves to dim_product.ProductID | Safe to enforce as FOREIGN KEY. |
| `fact_purchase_orders` | Referential integrity | SupplierID fully resolves to dim_supplier.SupplierID | Safe to enforce as FOREIGN KEY. |
| `fact_purchase_orders` | Referential integrity | WarehouseID fully resolves to dim_warehouse.WarehouseID | Safe to enforce as FOREIGN KEY. |
| `fact_purchase_orders` | Date sequence | 0 rows where ExpectedReceiptDate precedes OrderDate |  |
| `fact_purchase_orders` | Date sequence | 0 rows where ActualReceiptDate precedes OrderDate |  |
| `fact_purchase_orders` | Lead time distribution | Actual lead time: min 5, median 20, max 47 days. Planning lead time on dim_product: 5-30 days | Use actual lead time for supplier scoring, planning lead time for reorder point. |
| `fact_purchase_orders` | Defect rate range | 0 rows outside [0,1]; observed range 0.0002 to 0.1729 | Stored as a proportion; format as percentage in the model. |
| `fact_purchase_orders` | History span | OrderDate spans 2023-01-01 to 2026-08-31 (3.7 years) |  |
| `fact_sales_orders` | Primary key uniqueness | SalesOrderID is unique across 80,000 rows | Safe to enforce as PRIMARY KEY. |
| `fact_sales_orders` | Referential integrity | ProductID fully resolves to dim_product.ProductID | Safe to enforce as FOREIGN KEY. |
| `fact_sales_orders` | Referential integrity | WarehouseID fully resolves to dim_warehouse.WarehouseID | Safe to enforce as FOREIGN KEY. |
| `fact_sales_orders` | Date sequence | 0 rows where PromisedDeliveryDate precedes OrderDate |  |
| `fact_sales_orders` | Date sequence | 0 rows where ActualDeliveryDate precedes OrderDate |  |
| `fact_sales_orders` | History span | OrderDate spans 2023-01-01 to 2026-08-31 (3.7 years) |  |
| `fact_sales_orders` | Revenue concentration | 513 of 1,000 products (51.30%) generate 80% of revenue | A classical Pareto would put ~20% of SKUs in class A. Deviation here must inform the ABC thresholds chosen in Phase 9. |
| `fact_shipments` | Primary key uniqueness | ShipmentID is unique across 80,000 rows | Safe to enforce as PRIMARY KEY. |
| `fact_shipments` | Referential integrity | SalesOrderID fully resolves to fact_sales_orders.SalesOrderID | Safe to enforce as FOREIGN KEY. |
| `fact_shipments` | Referential integrity | WarehouseID fully resolves to dim_warehouse.WarehouseID | Safe to enforce as FOREIGN KEY. |
| `fact_shipments` | Date sequence | 0 rows where DeliveryDate precedes ShipDate |  |
| `fact_shipments` | Cross-table date sequence | 0 shipments dispatched before their sales order date |  |
| `fact_shipments` | Warehouse consistency | 0 shipments dispatched from a different warehouse than the order | Warehouse is redundant on shipments; source it from the order. |
| `fact_shipments` | Delivery date agreement | 0 shipments whose DeliveryDate differs from the sales order ActualDeliveryDate | The two tables agree; either may be used for OTIF. |
| `fact_shipments` | DeliveryStatus reproducibility | DeliveryStatus disagrees with (DeliveryDate > PromisedDeliveryDate) on 0 of 80,000 rows (0.00%) | Flag is fully reproducible; derive OTIF in DAX for transparency. |
| `fact_shipments` | History span | ShipDate spans 2023-01-02 to 2026-09-01 (3.7 years) |  |
| `fact_weekly_demand` | Primary key uniqueness | WeekStart+ProductID+WarehouseID is unique across 315,000 rows | Safe to enforce as PRIMARY KEY. |
| `fact_weekly_demand` | Referential integrity | ProductID fully resolves to dim_product.ProductID | Safe to enforce as FOREIGN KEY. |
| `fact_weekly_demand` | Referential integrity | WarehouseID fully resolves to dim_warehouse.WarehouseID | Safe to enforce as FOREIGN KEY. |
| `fact_weekly_demand` | Grain completeness | 105 weeks x 500 products x 6 warehouses = 315,000 expected; 315,000 actual (complete dense grid) | A dense grid means no gap-filling is required before forecasting. |
| `fact_weekly_demand` | Week alignment | WeekStart falls on: ['Monday'] |  |
| `fact_weekly_demand` | History span | WeekStart spans 2024-09-02 to 2026-08-31 (2.0 years) |  |
| `fact_weekly_demand` | Demand intermittency | 0.35% of all product-warehouse-weeks have zero demand; mean weekly demand 19.7 units | Low intermittency permits ETS/SARIMA. High intermittency would require Croston-style methods. |
| `fact_weekly_demand` | Demand variability (CV) | Coefficient of variation per product-warehouse series: p10=0.257, p25=0.274, p33=0.283, p50=0.303, p66=0.323, p75=0.337, p90=0.365 | These percentiles set defensible XYZ thresholds in Phase 10 instead of textbook constants. |
| `fact_weekly_demand` | Baseline forecast accuracy | MAE 2.575 units, WAPE 13.04%, bias -2.52% | This is the benchmark the Python model must beat in Phase 14. Recorded now so the comparison cannot be retrofitted. |
| `security_user_access` | Primary key uniqueness | UserEmail is unique across 7 rows | Safe to enforce as PRIMARY KEY. |
